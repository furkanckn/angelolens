/**
 * Shared UI primitives for Angelo Lens pages/sections.
 * Prefer these when adding new blocks so spacing, type, and CTAs stay consistent.
 *
 * - Section / SectionIntro / PageShell → layout rhythm
 * - ButtonLink → primary actions
 */
export { Section, SectionIntro, PageShell } from "./Section";
export { ButtonLink } from "./ButtonLink";
